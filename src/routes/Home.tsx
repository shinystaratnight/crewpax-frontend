import React from 'react'
import {
  View,
  Text,
  Button
} from 'react-native'

const Home = () => {
  return (
    <View>
      <Text>Home Screen</Text>
      <Button title="Sign Out" onPress={() => {}} />
    </View>
  )
}

export default Home
