import { StyleSheet } from 'react-native'

export const loginStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 18,
    paddingTop: 20,
  },
  textLink: {
    fontSize: 15,
    lineHeight: 22,
    fontWeight: '500',
    marginVertical: 10,
  },
})